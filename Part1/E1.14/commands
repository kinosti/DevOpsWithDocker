Commands:
> docker build . -t backend-updated && docker run -p 8080:8080 backend-updated
> docker build . -t frontend-updated && docker run -p 5000:5000 frontend-updated

The server was accessed via browser to localhost:5000.
